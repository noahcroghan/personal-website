version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    allow:
      - update-type: ["version-update:semver-patch", "version-update:semver-minor"]  # Safe updates only
    groups:
      npm-dependencies:
        patterns:
          - "*"
